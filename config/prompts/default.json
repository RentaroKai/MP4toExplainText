{
  "fields": {
    "Name of AnimationFile": {
      "description": "Name of the animation (in English, around 16 characters)",
      "type": "string",
      "required": true
    },
    "character_gender": {
      "description": "Suitable gender for the movement",
      "type": "string",
      "required": true,
      "options": ["male", "female", "any"]
    },
    "character_age_group": {
      "description": "Suitable age group for the movement",
      "type": "string",
      "required": true,
      "options": ["adult", "elderly", "child", "any"]
    },
    "character_body_type": {
      "description": "Suitable body type for the movement",
      "type": "string",
      "required": true,
      "options": ["average", "muscular", "Feminine glamour", "any"]
    },
    "Overall Movement Description": {
      "description": "Overall description of the movement (50-120 characters)",
      "type": "string",
      "required": true
    },
    "Initial Pose": {
      "description": "Starting pose (example: standing, on all fours, sitting on a chair, walking pose, etc.)",
      "type": "string",
      "required": true
    },
    "Final Pose": {
      "description": "Ending pose (example: standing, on all fours, sitting on a chair, walking pose, etc.)",
      "type": "string",
      "required": true
    },
    "Appropriate Scene": {
      "description": "Appropriate scene (e.g., daily life, combat, etc.)",
      "type": "string",
      "required": true
    },
    "Loopable": {
      "description": "The first and last poses match exactly in position and angle, making the loop unnoticeable even when played endlessly. e.g., Yes/No",
      "type": "string",
      "required": true,
      "options": ["Yes", "No"]
    },
    "Tempo Speed": {
      "description": "Movement tempo",
      "type": "string",
      "required": true,
      "options": ["Fast-paced", "Moderate", "Slow"]
    },
    "Intensity Force": {
      "description": "Movement intensity",
      "type": "string",
      "required": true,
      "options": ["High Impact", "Subtle"]
    },
    "Posture Detail": {
      "description": "Detailed description of posture (16-100 characters)",
      "type": "string",
      "required": true
   },
    "param_01": {
      "description": "HandyItem",
      "type": "string",
      "required": false
    },
    "param_02": {
      "description": "CominucationParam(e.g., Neutral, Agree, Deny,  etc.)",
      "type": "string",
      "required": false
    },
    "param_03": {
      "description": "Emotion(e.g., Neutral, Happy, Sad, Angry, etc.)",
      "type": "string",
      "required": false
    }
  }
} 